# --- ძირითადი სერვერი და ვებ ჩარჩო ---
fastapi
uvicorn

# --- AI/RAG ინსტრუმენტები ---
langchain-openai        # LangChain-ის ინტეგრაცია OpenAI-სთან (Embeddings, LLMs)
langchain-core          # LangChain-ის საბაზისო კლასები და სქემები
langchain-community     # LangChain-ის ინსტრუმენტები და ინტეგრაციები (მაგ., Document Loaders)

# --- ვექტორული ბაზა ---
chromadb                # ChromaDB-ის ბირთვი
# 📢 არ არის საჭირო: langchain-chroma-ს ნაცვლად გამოიყენება langchain-community-ში არსებული Chroma

# --- მონაცემთა დამუშავება და უსაფრთხოება ---
pypdf                   # PDF ფაილების წასაკითხად (როგორც main.py-ში, ასევე ingest.py-ში)
requests                # გარე HTTP მოთხოვნები (OpenAI API)
python-dotenv           # გარემოს ცვლადების (.env) ლოკალურად ჩასატვირთად
python-multipart        # (რეკომენდებულია) FastAPI-სთვის, თუ მომავალში ფაილების ატვირთვა დაგჭირდებათ

# --- მონაცემთა ვალიდაცია ---
pydantic                # FastAPI ავტომატურად იყენებს Pydantic-ს, მაგრამ მისი ცალკე მითითება არ არის შეცდომა
